[eiam]
instance_id = ""          # EIAM 实例 ID（必填）
region_id = "cn-hangzhou" # 阿里云区域
access_key_id = ""        # 阿里云 AccessKey ID
access_key_secret = ""    # 阿里云 AccessKey Secret
timeout = 30              # 请求超时（秒）
